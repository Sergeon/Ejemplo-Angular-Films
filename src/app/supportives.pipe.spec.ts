import { SupportivesPipe } from './supportives.pipe';

describe('SupportivesPipe', () => {
  it('create an instance', () => {
    const pipe = new SupportivesPipe();
    expect(pipe).toBeTruthy();
  });
});
